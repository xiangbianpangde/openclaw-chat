# JJC-20260412-002 泰迪杯项目 AI 助手集成实施计划

## 📜 奉天承运 中书省拟定

**任务 ID:** JJC-20260412-002  
**拟定机构:** 中书省  
**拟定时间:** 天启二年四月十二日 (2026 年 4 月 12 日) 00:27  
**中书令:** 谨拟

---

## 一、皇上旨意

**任务:** 为泰迪杯项目添加 AI 助手，接入 API

**时限:** 20 分钟内完成方案拟定

---

## 二、问题诊断

### 2.1 当前状态

| 模块 | 状态 | 说明 |
|------|------|------|
| task1 代码 | ✅ 存在 | 数据库构建 |
| task2 代码 | ✅ 存在 | assistant.py, sql_generator.py |
| task3 代码 | ✅ 存在 | knowledge_base.py, intent_planner.py, attribution.py |
| API Key 配置 | ❌ 缺失 | openclaw.json 中无 API Key |
| AI 助手集成 | ⚠️ 部分 | task2 已集成 LLM，task3 未集成 |

### 2.2 代码分析

**task2 (SQLGenerator):**
- ✅ 已集成 LLM (DashScope API)
- ✅ API Key 从环境变量读取
- ✅ 自然语言转 SQL

**task3 (IntentPlanner):**
- ❌ 使用规则匹配，无 LLM
- ❌ 意图识别准确率有限
- ❌ 需要集成 LLM 提升效果

**task3 (AttributionAnalyzer):**
- ❌ 使用规则引擎，无 LLM
- ❌ 归因报告模板化
- ❌ 需要集成 LLM 生成自然语言报告

### 2.3 配置文件

**当前 config/openclaw.json:**
```json
{
  "data_dir": "/root/.../raw",
  "db_path": "/root/.../teddy_cup.db",
  "result_dir": "/root/.../result"
}
```

**缺少字段:**
- api_key
- api_base
- model

---

## 三、详细实施计划

### 3.1 阶段一：API Key 配置（10 分钟）

**任务:**
1. 更新 config/openclaw.json
2. 添加 API Key 配置
3. 验证 API 连接

**配置格式:**
```json
{
  "data_dir": "/root/.../raw",
  "db_path": "/root/.../teddy_cup.db",
  "result_dir": "/root/.../result",
  "api_key": "sk-sp-xxx",
  "api_base": "https://dashscope.aliyuncs.com/v1",
  "model": "qwen3-coder-plus"
}
```

### 3.2 阶段二：AI 助手集成（30 分钟）

#### task2: SQLGenerator 增强

**当前状态:** ✅ 已集成 LLM

**增强内容:**
- 改进 prompt 模板
- 添加 SQL 验证
- 添加错误重试机制

#### task3: IntentPlanner 集成 LLM

**当前状态:** ❌ 规则匹配

**集成方案:**
```python
class IntentPlanner:
    def __init__(self, config: dict):
        self.api_key = config.get('api_key')
        self.base_url = config.get('api_base')
        self.model = config.get('model')
    
    def parse_intent_with_llm(self, query: str) -> List[Intent]:
        """使用 LLM 进行意图识别"""
        prompt = f"""分析以下问题的意图：
问题：{query}

请识别：
1. 意图类型（查询/对比/归因/趋势）
2. 实体信息（公司、指标、时间）
3. 子任务分解

以 JSON 格式返回。"""
        
        response = self._call_llm(prompt)
        return self._parse_response(response)
```

#### task3: AttributionAnalyzer 集成 LLM

**当前状态:** ❌ 规则引擎

**集成方案:**
```python
class AttributionAnalyzer:
    def generate_report_with_llm(self, result: AttributionResult) -> str:
        """使用 LLM 生成自然语言归因报告"""
        prompt = f"""基于以下归因分析结果，生成自然语言报告：

公司：{result.stock}
指标：{result.metric}
时期：{result.period}
变化率：{result.change_rate:.1f}%

归因因素：
{self._format_factors(result.factors)}

请生成一份专业的归因分析报告。"""
        
        return self._call_llm(prompt)
```

### 3.3 阶段三：测试验证（20 分钟）

**测试内容:**
1. task1 运行测试
2. task2 AI 查询测试
3. task3 归因分析测试
4. API 调用验证

**测试用例:**
```python
# task2 测试
generator = SQLGenerator(config)
sql = generator.generate("金花股份 2025 年的营收是多少")
assert "SELECT" in sql

# task3 测试
planner = IntentPlanner(config)
intents = planner.parse_intent("查询金花股份的营收并分析增长原因")
assert len(intents) >= 2

analyzer = AttributionAnalyzer(config)
report = analyzer.generate_report(result)
assert "增长" in report or "下降" in report
```

### 3.4 阶段四：文档更新（10 分钟）

**更新文件:**
1. README.md - 添加 AI 助手说明
2. config/openclaw.json.example - API 配置示例
3. docs/AI_ASSISTANT.md - AI 助手使用指南

---

## 四、API 集成方案

### 4.1 API 选择

**推荐:** DashScope (通义千问)

**理由:**
- ✅ task2 已使用
- ✅ 中文支持好
- ✅ 财务领域知识丰富
- ✅ 成本较低

**备选:** OpenAI GPT-4

### 4.2 配置管理

**配置文件:** config/openclaw.json

**环境变量:**
```bash
export DASHSCOPE_API_KEY="sk-sp-xxx"
export DASHSCOPE_BASE_URL="https://dashscope.aliyuncs.com/v1"
export DASHSCOPE_MODEL="qwen3-coder-plus"
```

### 4.3 API 调用封装

```python
class LLMClient:
    def __init__(self, config: dict):
        self.api_key = config.get('api_key')
        self.base_url = config.get('api_base')
        self.model = config.get('model')
    
    def chat(self, prompt: str, **kwargs) -> str:
        """调用 LLM API"""
        import requests
        
        headers = {
            'Authorization': f'Bearer {self.api_key}',
            'Content-Type': 'application/json'
        }
        
        data = {
            'model': self.model,
            'messages': [{'role': 'user', 'content': prompt}],
            **kwargs
        }
        
        response = requests.post(
            f'{self.base_url}/chat/completions',
            headers=headers,
            json=data
        )
        
        return response.json()['choices'][0]['message']['content']
```

---

## 五、代码修改清单

### 5.1 修改文件

| 文件 | 修改内容 | 行数变化 |
|------|----------|----------|
| config/openclaw.json | 添加 API 配置 | +5 |
| src/task2/sql_generator.py | 改进 prompt | +20 |
| src/task3/intent_planner.py | 集成 LLM | +100 |
| src/task3/attribution.py | 集成 LLM 报告 | +50 |
| src/task3/__init__.py | 导出 LLMClient | +10 |
| README.md | 添加 AI 说明 | +50 |

### 5.2 新增文件

| 文件 | 功能 |
|------|------|
| src/utils/llm_client.py | LLM API 调用封装 |
| config/openclaw.json.example | 配置示例 |
| docs/AI_ASSISTANT.md | AI 助手使用指南 |

---

## 六、测试方案

### 6.1 单元测试

```python
def test_sql_generator():
    config = load_config()
    generator = SQLGenerator(config)
    sql = generator.generate("金花股份 2025 年的营收")
    assert "core_performance_indicators" in sql

def test_intent_planner():
    config = load_config()
    planner = IntentPlanner(config)
    intents = planner.parse_intent("查询营收并分析原因")
    assert len(intents) == 2

def test_attribution_analyzer():
    config = load_config()
    analyzer = AttributionAnalyzer(config)
    report = analyzer.generate_report(result)
    assert len(report) > 100
```

### 6.2 集成测试

```python
def test_full_pipeline():
    # 完整流程测试
    query = "金花股份 2025 年营收是多少，为什么增长"
    
    # 意图识别
    intents = planner.parse_intent(query)
    
    # SQL 生成
    sql = generator.generate(query)
    
    # 执行查询
    results = execute_sql(sql)
    
    # 归因分析
    report = analyzer.generate_report(results)
    
    assert report is not None
```

---

## 七、时间安排

| 阶段 | 任务 | 耗时 | 累计 |
|------|------|------|------|
| 阶段一 | API Key 配置 | 10 分钟 | 10 分钟 |
| 阶段二 | AI 助手集成 | 30 分钟 | 40 分钟 |
| 阶段三 | 测试验证 | 20 分钟 | 60 分钟 |
| 阶段四 | 文档更新 | 10 分钟 | 70 分钟 |

**总预计耗时:** 70 分钟

---

## 八、验收标准

| 验收项 | 标准 | 验证方法 |
|--------|------|----------|
| API Key 配置 | config 文件包含 api_key | 检查配置文件 |
| task2 AI 查询 | 自然语言转 SQL 正常 | 测试用例 |
| task3 意图识别 | LLM 意图识别正常 | 测试用例 |
| task3 归因报告 | 自然语言报告生成 | 测试用例 |
| API 调用 | API 调用成功 | 日志验证 |
| 文档完整 | README + 使用指南 | 检查文档 |

---

## 九、交付物清单

1. **中书省:** `中书省/JJC-20260412-002-实施计划.md` (本文档)
2. **门下省:** `门下省/JJC-20260412-002-审核意见书.md`
3. **尚书省:** `尚书省/JJC-20260412-002-执行报告.md`
4. **尚书省:** `config/openclaw.json` (API 配置)
5. **尚书省:** `src/utils/llm_client.py` (LLM 客户端)
6. **尚书省:** `docs/AI_ASSISTANT.md` (使用指南)
7. **太子:** `太子/JJC-20260412-002-回奏皇上.md`

---

## 十、结语

本方案为泰迪杯项目 AI 助手集成的指导方案，请门下省速速审核，如无异议则封发尚书省执行。

**中书省保证:** 方案已详细定义 API 配置、代码修改、测试方案，尚书省可按方案执行。

---

**中书令 谨拟**  
天启二年四月十二日 (2026 年 4 月 12 日) 00:27

---

## 附件

- 配置文件：config/openclaw.json
- 预计耗时：70 分钟
- API: DashScope (通义千问)
