name: openclaw_im_v2
description: OpenClaw IM 客户端 v2 - 直接集成 OpenClaw Gateway WebSocket
publish_to: 'none'
version: 4.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.2
  webview_flutter: ^4.4.2
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
